# This contains a function to calculate the area of a rectangle.

def area(length, width): # Calculate the area of a rectangle
    return length * width # Return the value of length and width

# This contains a function to calculate the perimeter of a rectangle.

def perimeter(length, width): # Calculate the perimeter of a rectangle
    return 2 * (length + width) # Return the value of the perimeter